.env
bike_alert.log
__pycache__/
*.pyc
.venv/
venv/
# NOTE: seen_ads.json, stats.json and prices.json are intentionally
# NOT ignored — GitHub Actions commits them back to preserve state
# (deduplication, weekly heartbeat history, deal-scoring medians).
.DS_Store
